import React from 'react';
import {Screen} from '@/ui/Screen';

/** Placeholder — replaced in its own task. */
export function ExerciseHistoryScreen() {
  return <Screen title="Exercise history" />;
}
